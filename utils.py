from aiogram import types, F, Router
from aiogram.types import Message
from aiogram.enums import ParseMode

import kb
import text

#Функция с проверкой
"""
def profile(msg):
    if ():
        msg.answer(text.T_profile.format(name=msg.from_user.full_name, group="NONE"),parse_mode=ParseMode.MARKDOWN_V2, reply_markup=kb.inl_setgroup)
    else:
        msg.answer(text.T_profile.format(name=msg.from_user.full_name, group="NONE"), parse_mode=ParseMode.MARKDOWN_V2, reply_markup=kb.inl_setgroup)
"""